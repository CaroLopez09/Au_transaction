meta {
  name: 01 Listar
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/recipients
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenApprover}}
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
