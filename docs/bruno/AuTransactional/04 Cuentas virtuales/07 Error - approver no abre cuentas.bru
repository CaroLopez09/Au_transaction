meta {
  name: 07 Error - approver no abre cuentas
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/virtual-accounts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenApprover}}
}

body:json {
  {
    "mode": "fiat"
  }
}

tests {
  test("responde 403", function () {
    expect([403]).to.include(res.getStatus());
  });
}
