meta {
  name: 02 Alta minima en Kira
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/onboarding
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenCompliance}}
}

body:json {
  {
    "businessLegalName": "Juriscop S.A.S.",
    "email": "finanzas@juriscop.co",
    "sourceOfFunds": "sales_of_goods_and_services"
  }
}

tests {
  test("responde 201 o 503", function () {
    expect([201, 503]).to.include(res.getStatus());
  });
}

docs {
  Llama a `POST /v1/users`. Sin credenciales de Kira: 503 `kira_not_configured`.
}
