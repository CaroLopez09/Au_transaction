meta {
  name: 10 Error - sin token
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: none
}

tests {
  test("responde 401", function () {
    expect([401]).to.include(res.getStatus());
  });
}

docs {
  Sin cabecera Authorization responde 401 (F7): el 403 queda para el rol autenticado sin permiso.
}
