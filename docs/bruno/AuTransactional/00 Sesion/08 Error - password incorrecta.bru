meta {
  name: 08 Error - password incorrecta
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@juriscop.test",
    "password": "incorrecta"
  }
}

tests {
  test("responde 422", function () {
    expect([422]).to.include(res.getStatus());
  });
}

docs {
  Mismo 422 que con un correo inexistente: no revela que cuentas existen.
}
