meta {
  name: 07 Quien soy
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
