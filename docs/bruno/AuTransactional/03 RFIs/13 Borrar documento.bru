meta {
  name: 13 Borrar documento
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/api/rfis/{{rfiId}}/items/{{rfiDocItemId}}/documents/{{rfiDocumentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenAdmin}}
}

docs {
  Kira no deja borrar el ultimo archivo de un item respondido (422).
}
