meta {
  name: 02 Solo abiertos
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/rfis?open=true
  body: none
  auth: bearer
}

params:query {
  open: true
}

auth:bearer {
  token: {{tokenReadOnly}}
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
