meta {
  name: 07 Resultado liveness
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/liveness/session/{{livenessSessionId}}/result
  body: none
  auth: none
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
