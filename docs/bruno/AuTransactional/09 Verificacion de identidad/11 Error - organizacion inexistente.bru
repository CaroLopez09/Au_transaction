meta {
  name: 11 Error - organizacion inexistente
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/number-challenge/session?clientId=nadie
  body: none
  auth: none
}

params:query {
  clientId: nadie
}

tests {
  test("responde 401", function () {
    expect([401]).to.include(res.getStatus());
  });
}
