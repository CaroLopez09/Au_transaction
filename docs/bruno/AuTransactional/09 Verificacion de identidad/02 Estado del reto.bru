meta {
  name: 02 Estado del reto
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/number-challenge/{{challengeId}}/status
  body: none
  auth: none
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
