meta {
  name: 10 Error - liveness con clientId
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/liveness/session?clientId={{empresa}}
  body: none
  auth: none
}

params:query {
  clientId: {{empresa}}
}

tests {
  test("responde 422", function () {
    expect([422]).to.include(res.getStatus());
  });
}
