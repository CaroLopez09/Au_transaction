meta {
  name: 02 Clientes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/platform/tenants
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenPlataforma}}
}

tests {
  test("responde 200 con todas las organizaciones", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().length).to.be.greaterThan(0);
  });
}
