meta {
  name: 02 Listar
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/payouts?limit=50
  body: none
  auth: bearer
}

params:query {
  limit: 50
}

auth:bearer {
  token: {{tokenApprover}}
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
