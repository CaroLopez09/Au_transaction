meta {
  name: 03 Detalle
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/payouts/{{payoutId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{tokenReadOnly}}
}

tests {
  test("responde 200", function () {
    expect([200]).to.include(res.getStatus());
  });
}
